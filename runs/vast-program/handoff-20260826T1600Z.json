{
  "state_at_note": "NOTHING IS RUNNING. Both lanes are free, runs/vast-program/controller.lock is absent, the GPU is idle. If the lock IS present, read it before assuming it is stale. Last phases: phase8-base-code-baseline-humaneval passed 15:41Z, phase8-code-harness-oracle-mbpp and -humaneval passed 15:47Z. Nothing has run on the box since.",

  "phase_8_is_STILL_blocked_on_one_operator_command": "bash ops/vast/install_supervisor.sh. RE-CHECKED THIS TURN at 16:01Z: /usr/local/bin/daedalus-approved branch still answers 'unapproved command branch'. Step 1 of phase 8 is a branch switch and a session cannot run git checkout; the command exists in ops/vast/run-approved (a9983ad, f4e2adf) but the INSTALLED copy predates it. The same step restores pr-find and reload-service. It is an operator step BY DESIGN -- a wrapper that changed when a branch edited a file would let a session widen its own permissions -- so do not look for a way around it. CHECK IT FIRST every turn with `daedalus-approved branch`: if it prints a SHA, phase 8 step 1 is open and the operator lane transitions to passed. Blocked since 13:47Z, i.e. ~2.2h of idle GPU at this note.",

  "what_shipped_this_turn": "1fc855a only. The trainer computed per-source held-out BPB at every eval interval and logged only the blend of it: evaluate_bpb_mixture does a full holdout pass PER SOURCE and returns both, and Trainer._val_bpb took ['val_bpb'] and dropped the rest. Zero compute was saved by discarding it -- the per-source passes are where the cost is and they ran either way. It matters from phase 8 on because a continued-pretraining arm is gated on code BPB and general replay BPB read INDEPENDENTLY, and the blend is the one number that cannot answer that: a code gain and a replay regression move it in opposite directions and it reports their sum. metrics.jsonl now carries val_bpb_per_source beside val_bpb. _val_bpb -> _validate (returns the result, not one key of it). FOCUSED: two new tests in tests/test_train.py. AFFECTED: test_train/test_abl_arch/test_qat 285 passed; test_eval/test_bpb_eval/test_corpus_gate/test_mixture_opt green beside them. Full suite run this turn -- see full_suite below.",

  "it_was_verified_on_the_real_corpus_not_only_on_fixtures": "The repeated failure pattern on this branch is a fixture written to the code rather than to reality, so the change was also run live: one training step at the released 49,152 vocabulary against data/holdout (the real 3-source tree), config tok-probe-49152, the same shape phase 7 mixture probes used. MEASURED, runs/smoke-per-source-bpb/metrics.jsonl: fineweb-edu 3.5403, dclm-baseline 3.6914, stack-edu-python 4.9279, blend val_bpb 3.7916. The blend reproduces from the parts under the sampler weights it was launched with (3.540292*0.688114 + 3.691384*0.146739 + 4.927904*0.165147 = 3.79162), which is the check that the two numbers describe one measurement. The model is untrained at step 1 (loss 11.22 against ln(49152)=10.80) so these are NOT quality figures -- they verify the plumbing on real shards. What they do show is that code sits ~1.4 bits/byte above general web from step one, which is the separation the blend hides and phase 8 must read.",

  "full_suite": "1995 passed, 4 skipped (the four hero-split skips; nothing regressed). Run this turn because the change touches train.py, which everything imports.",

  "disk_is_at_91_percent": "84G available of 915G at 16:15Z. Not a blocker now, but phase 8's code corpus is 65% of a multi-billion-token mix and the fetch is the next large writer, so check df BEFORE starting it and plan the incremental-upload-then-delete path from step one rather than after. runs/smoke-per-source-bpb/ holds a 337MB milestone checkpoint from this turn's live check that can be deleted by anyone with rm (this session did not have it).",

  "why_that_change_is_on_the_optimization_branch": "train.py is a shared file and the plan's rule is that shared work goes to the optimization branch first and forward-merges into the code branch. It is NOT the phase 8 gate instrument: scripts/bpb_eval.py already scores per-source BPB out of band on an immutable checkpoint, which is the right thing to decide a gate on. This is the DURING-RUN signal, so an arm already past the 1.5% replay bound says so at its first interval instead of after 1B tokens. Recorded as a decision note under phase8-prep-shared-metrics.",

  "do_not_retry_the_contamination_coverage_gap": "MEASURED AND CLOSED AS UNCLOSABLE this turn, recorded as a finding note under phase7-contamination-coverage. The 157,921,561 unscanned fineweb-edu tokens in the corpus-contamination FAIL cannot be closed on this box. data/shards/fineweb-edu/manifest.json holds 479,185,034 tokens locally and subset_of.total_tokens 5,193,493,853; corpus_gate.source_supply defaults to the BUILT figure, so supply is the Hub-side released build. runs/preflight/contam-exposure.json (source_tokens 5,035,572,292) is an artifact of the ORIGINAL build box carried in by the Aug 24 bootstrap snapshot. So the gap is between two original-build artifacts -- the scan walked fineweb-edu at 5.036B while the source finished at 5.193B, i.e. THE SCAN RAN BEFORE THAT SOURCE FINISHED BUILDING. Those 158M tokens were never on this box. A local re-scan would record 479M and make the criterion fail by 4.7B instead of 158M. DO NOT SPEND CPU RE-RUNNING contam_scan AGAINST data/shards TO CLOSE THIS.",

  "next_1": "Check `daedalus-approved branch`. If installed: phase 8 step 1 -- `daedalus-approved branch vast/daedalus-code-20260824` prints the parent SHA for the code run manifest, then `pr-draft` a stacked draft PR onto vast/daedalus-improvements-20260824 BEFORE any code-specific implementation, then commit the untracked baseline scorecards in runs/eval/code-base/ and runs/eval/code-base-oracle/ there -- they are Daedalus-Code results, they follow the checkout.",

  "next_2": "Then daedalus/codeprep.py and the code corpus: 65% permissively licensed code (Python 55%, JS/TS 12%, C/C++ 10%, Rust 8%, Go 6%, Java 5%, shell/SQL/other 4%), 15% technical/math prose, 20% general replay, split train/holdout BY REPOSITORY, decontaminated against HumanEval+/MBPP+ prompts, solutions, tests and repo metadata. It MUST pack under the 49,152 vocabulary -- phase 8 continues from released weights -- and train.py's vocabulary guard now refuses a mix-up rather than training through it. The fetch is the long pole and is CPU/network, so it runs on the inspect lane beside a GPU phase.",

  "a_judgement_call_left_open_deliberately": "The code-benchmark decontamination index is the one shared/code-specific boundary this turn did NOT cross. daedalus/eval_index.py and scripts/decontam_index.py are phase 2/7 tools on the optimization branch and take clean injection points (loader, expected_tasks, expected_splits, expected_items), so extending them to a second task family is arguably shared infrastructure. But the plan lists HumanEval+/MBPP+ decontamination under Phase 8 Data Design, and the stacked PR is specified to contain ONLY codeprep, code evaluation, code training/post-training and code-specific docs. It was left for the code branch rather than muddying the optimization diff on a blurry line. Reverse that only deliberately.",

  "why_step_9_at_scale_was_not_started_instead": "It is V2-only, trains nothing in this program, and a full rebuild re-streams from the Hub -- so it would contend for exactly the CPU and network that phase 8's code-corpus fetch needs, and phase 8's own handoff instruction is to prefer phase 8 when they contend. Starting a many-hour rebuild that phase 8 would then have to compete with is the wrong trade while phase 8 is one operator command from starting. It remains open and remains a judgement call, not a gate.",

  "trap_the_trainer_commits_its_own_metrics": "train.py auto-commits runs/<name>/metrics.jsonl and milestone.json as it runs -- this turn's live check produced ff2c30b 'smoke-per-source-bpb: step 1, 131072 tokens' by itself, before any session commit. So `commit-push -- runs/<name>/metrics.jsonl` afterwards stages nothing and the file is silently absent from YOUR commit while being present in the branch. That reads as a failed commit and is not one. Check with `git log -- <path>` before concluding a run artifact did not land; the .pt checkpoints are correctly excluded throughout.",

  "traps_still_current": "THE APPROVED WRAPPER MUST BE CALLED BY ABSOLUTE PATH: /usr/local/bin/daedalus-approved. Denied to this session's Bash: python -c, rm, cat, ls outside the repo, sleep/polling loops, compound commands with ; or &&, `bash ops/vast/run-approved`. Allowed: ls/head/tail/sed/grep/wc/date inside the repo, git log/show/status/diff, and the wrapper. `daedalus-approved test` with no -k runs the full suite and takes well past a 120s foreground timeout -- run it in the background and read the task output file. A phase log is APPENDED across runs, so the tail you read may be the previous run's; check the artifact's mtime with ls -l before believing a number. `phase --lane inspect run-phase ...` puts --lane BEFORE the subcommand. code_eval.py prints nothing until it finishes.",

  "the_heartbeat_still_cannot_show_this_blocker": "UNCHANGED from the 15:20Z note and still true. daedalus_progress imported github_progress.py when it started, so its five-minute tick renders neither the lane section nor the action banner however current the file on disk is. `daedalus-approved progress-once` renders one correct tick and the stale service overwrites it five minutes later; reload-service is blocked by the same install. DO NOT promote the blocker to the top-level status to make it louder -- session_keeper.program_has_stopped treats a top-level `blocked` carrying a blocker as the program having stopped and would take the keeper offline, and one without a blocker makes the keeper's own _block early-return without recording its reason. The side lane is the only place this sits safely, and the user-facing turn report is the channel that actually reaches anyone right now.",

  "deadline": "Program started 2026-08-24T10:44:35Z. At this note ~T+53.3h of 144h; finalization opens at T+136h = 2026-08-30T02:44Z, leaving ~82.7h. Phases 0-6 complete. Phase 7 complete except step 9 at scale. Phase 8 blocked at step 1, with its base baselines, its harness and both oracles already measured, and its shared trainer metric now in place. The plan's 104-136h window for Daedalus-Code is a late bound, not a plan."
}
