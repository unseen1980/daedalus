{
  "state_at_note": "NOTHING IS RUNNING. Both lanes are free and runs/vast-program/controller.lock is absent; if it is present, read it before assuming it is stale. The GPU is idle. Last phases: phase8-base-code-baseline-humaneval passed 15:41Z, phase8-code-harness-oracle-mbpp and -humaneval passed 15:47Z.",

  "phase_8_is_still_blocked_on_one_operator_command": "bash ops/vast/install_supervisor.sh. Step 1 is a branch switch and a session cannot run git checkout; `daedalus-approved branch` exists in ops/vast/run-approved but the INSTALLED copy at /usr/local/bin/daedalus-approved predates it and still answers 'unapproved command branch'. Same step unblocks pr-find and reload-service. It is an operator step by design: a wrapper that changed when a branch edited a file would let a session widen its own permissions. CHECK IT FIRST with `daedalus-approved branch` -- if it prints a SHA, phase 8 step 1 is open, and transition the operator lane to passed.",

  "the_blocker_is_recorded_two_ways_and_NEITHER_reaches_the_5_minute_heartbeat_yet": "THE RUNNING PUBLISHER IS A STALE PROCESS. daedalus_progress imported github_progress.py when it started, so its heartbeat renders neither the lane section nor the action banner no matter how current the file on disk is -- the earlier STATUS.md that did show lanes was a manual progress-once, and the service overwrote it at its next tick. Only a fresh process reads the new code: `daedalus-approved progress-once` (one tick, then overwritten) or reload-service, which is blocked by the same install. So: (1) a lane record, `phase --lane operator transition --phase phase8-branch-prerequisite --status blocked` with user_action_required true, which any current publisher renders straight from state.json with no new code; (2) commit 126f320, which compares each installed control-plane file against HEAD -- not the working tree, so a session cannot make the heartbeat demand its own uncommitted wrapper -- and names what is stale. Run against this box it finds exactly one file, ops/vast/run-approved. DO NOT promote this to the top-level status to make it louder: session_keeper.program_has_stopped treats a top-level `blocked` carrying a blocker as the program having stopped and would take the keeper offline, and a top-level `blocked` WITHOUT one makes the keeper's own _block early-return without recording its reason. The side lane is the only place this can sit safely.",

  "what_shipped_this_turn": "126f320 the heartbeat's stale-control-plane check. c058063 MBPP+'s base suite and its extraction. 97c7237 problems with no extended inputs. eb912ec three ways the harness failed the reference solutions. f16da9a repr(float('inf')) is not a literal, plus the detail of whichever suite failed. 5f90851 keep what precedes the first def. 63908c9 pr-body. FOCUSED AND AFFECTED SUITES for every change: tests/test_code_eval.py 57 passed, plus scorecard/gate/eval-command/gguf/retrieval/bpb groups, and the control-plane group at 192 passed. The full suite has NOT been run this turn.",

  "mbpp_plus_had_never_run_and_five_defects_sat_under_it": "--dataset mbpp-plus was an advertised choice that raised on its first problem. (1) MBPP+ ships no `test` key; its base suite is base_input scored against the reference. (2) Extraction cut at the first column-zero line, which is exactly where a comment inside a function body sits -- Mbpp/64's own reference lost its whole body and scored a syntax error. (3) re.Match compares by identity, so three references failed against themselves with the assertion printing two identical spans. (4) An input the reference itself rejects failed every candidate (Mbpp/404, min(None, 3)). (5) repr(float('inf')) is the NAME `inf`, so every problem whose extended inputs reach the floating-point extremes died on NameError before any comparison -- invisible while the base suite passes nothing, and it would have surfaced as a mysteriously low pass@1_plus on the first arm that started solving problems. ALL FIVE WERE INVISIBLE TO THE TEST SUITE because every fixture was written to the code rather than to the dataset. That is the pattern the phase 7 handoff named, found again.",

  "the_oracle_is_the_standing_check": "`--oracle` scores the benchmark's own reference solutions through the same extraction, sandbox and scoring rule, writing <dataset>-oracle with the dataset as the artifact so it can never be read back as a model's score. MEASURED, runs/eval/code-base-oracle/: MBPP+ pass@1 1.0000, pass@1_plus 1.0000, syntax_valid 1.0000 on 378 of 378 (from 0.9894/0.9841/0.9974 after the first three fixes, and from not running at all before that); HumanEval+ 1.0000/1.0000 on 164. RUN IT AGAIN AFTER ANY CHANGE TO THIS HARNESS. A model's 0.000 is only a fact about the model once the references score 1.000 on the same path. Known bound: Mbpp/255 expands combinations_with_replacement near the 30s/1GB sandbox limit and scored fail_resource_limit on one run and passed on the next -- it is a bound, visible as a category, and it applies to every model equally.",

  "phase_8_untouched_base_baselines": "runs/eval/code-base/. HumanEval+ 164 items: pass@1 0.0000, syntax_valid 1.0000, 159 assertion failures and 5 exceptions. MBPP+ 378 items: pass@1 0.0079 (three problems solved), pass@1_plus 0.0053, syntax_valid 0.2381, 289 syntax errors. THESE ARE DAEDALUS-CODE RESULTS AND BELONG ON THE CODE BRANCH -- they are untracked, they follow the checkout, commit them there. Provenance: released base sha256 cfbf27dc, the digest phase 2 scored.",

  "what_the_baselines_do_to_the_gate": "RECORDED BEFORE ANY ARM IS TRAINED, and no preregistered threshold is moved. HumanEval+ pass@1 has a floor of exactly 0.000, so one solved problem in 164 clears 'improves over base' and a null result says nothing; its syntax validity is already 1.000 and CANNOT rise, because the prompt supplies the def and the extraction prepends it, so the program always parses. MBPP+ is the opposite and is where the power is: pass@1 0.0079 can fall as well as rise, and syntax validity 0.2381 has real headroom. The base model's MBPP+ failure is that it emits a def and then unindented body lines -- checked item by item, not an extraction artifact. So: read syntax validity on MBPP+, read HumanEval+ pass@1 as a strict floor, and code BPB's >=5% bar still carries the phase.",

  "next_1": "Check `daedalus-approved branch`. If installed: phase 8 step 1 -- `daedalus-approved branch vast/daedalus-code-20260824` prints the parent SHA for the code run manifest, then `pr-draft` a stacked draft PR onto vast/daedalus-improvements-20260824 BEFORE any code-specific implementation, then commit the baseline scorecards there.",

  "next_2": "Then daedalus/codeprep.py and the code corpus: 65% permissively licensed code (Python 55%, JS/TS 12%, C/C++ 10%, Rust 8%, Go 6%, Java 5%, shell/SQL/other 4%), 15% technical/math prose, 20% general replay, split train/holdout BY REPOSITORY, decontaminated against HumanEval+/MBPP+ prompts, solutions, tests and repo metadata. It MUST pack under the 49,152 vocabulary. The fetch is the long pole and is CPU/network, so it runs on the inspect lane beside a GPU phase.",

  "next_3": "DONE, no action needed: all four scorecards now come from one harness version, and both oracles were re-certified at it (ce46608, which is the same harness code as 5f90851 -- the commits between touch only prose). Both baselines were re-scored after the last extraction change and came back IDENTICAL in every metric; only the item_digests moved, because the change altered the recorded solution text for some completions without changing a single outcome. That is the evidence it removed a false-negative path rather than shifting the measurement, and it is not something the diff could have told you. RE-RUN --oracle AFTER ANY FURTHER CHANGE TO scripts/code_eval.py, and re-baseline if it moves.",

  "traps_still_current": "THE APPROVED WRAPPER MUST BE CALLED BY ABSOLUTE PATH: /usr/local/bin/daedalus-approved. Denied to this session's Bash: python -c, rm, cat, ls outside the repo, sleep/polling loops, compound commands with ; or &&, `bash ops/vast/run-approved`. Allowed: ls/head/tail/sed/grep/wc/date inside the repo, git log/show/status/diff, and the wrapper. To WAIT without sleep: `tail -n 0 -f <log> | grep -m 1 wrote` blocks until the line appears. A phase log is APPENDED across runs -- the tail you read may be the previous run's, so check the artifact's mtime with ls -l before believing a number. `phase --lane inspect run-phase ...` puts --lane BEFORE the subcommand. code_eval.py prints nothing until it finishes.",

  "deadline": "Program started 2026-08-24T10:44:35Z. At this note ~T+52.7h of 144h; finalization opens at T+136h = 2026-08-30T02:44Z, leaving ~83h. Phases 0-6 complete, phase 7 complete except step 9 at scale (a judgement call, V2-only, trains nothing -- prefer phase 8 if they contend), phase 8 blocked on the operator step above with its base baselines and its harness now measured."
}
