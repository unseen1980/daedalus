{
  "state_at_note": "NOTHING IS RUNNING. The main lane is free -- phase7-mixture-candidates passed 12:55Z, phase7-mixture-score-candidates passed 13:42Z, phase7-mixture-report passed after it. The GPU is idle. runs/vast-program/controller.lock should be absent; if it is present, read it before assuming it is stale. The inspect lane is free too.",

  "shipped_this_turn": "e8c9658 cap_saturation, recorded by derive and carried onto the arm it qualifies. 4b4a4aa dataprep --tokenizer and the source-manifest fingerprint. 44fdcce the trainer's vocabulary guard. 4c5efc6 the evaluator's tokenizer guard. 22254d4 the provenance criterion that could not pass. 7393ec0 the rebuilt tree, scanned and gated. f48c3ed the acceptance page. ba01429 the mixture verdict. 9a1ed14/a220904/f5844c7 pr-body. FULL SUITE covering every code change this turn: 1955 passed, 4 skipped (the four hero-split skips; nothing regressed). The commits after it are artifacts and prose.",

  "phase_7_steps_7_and_8_are_closed": "VERDICT: keep-baseline. Both candidates admissible -- no floor violation, no source past the 5% regression bound -- and neither clears the preregistered 0.5% minimum aggregate gain: quality-heavy 0.0810%, derived 0.0572%, against a 1.2486 baseline. That is the plan's own instruction for a proxy that cannot separate the arms. Per source they separate clearly: code BPB 0.9319 baseline / 0.9005 quality-heavy / 0.8879 derived (best to the arm with the largest code share), paid for on raw web 1.3828 / 1.4035 / 1.3899. Under the blueprint weighting -- raw web 32.6%, code 13.0% -- that trade is worth 0.06-0.08%. runs/corpus/mixture-verdict-probe.json and .md.",

  "the_cap_caveat_now_in_the_verdict": "stack-edu-python's 0.21257 bits/byte of excess asks for 8.38x its blueprint share and EXCESS_RATIO_CAP grants 2x, so the derived arm's 0.178 code share is the cap's answer, not a measured optimum -- and 2.1x and 84x both arrive as 2.0x, so the number cannot be read back. daedalus.mixture_opt.cap_saturation names saturated sources with the ask beside what was granted; derive records it; the verdict page prints it under 'Shares the cap set, not the measurement'. DISCLOSURE ONLY: no preregistered threshold moved, no arm reclassified. For the derivation the candidate arms were launched from -- written before the field existed -- the caveat is recomputed at read time from the excess loss it already carries, because that artifact is the launch record the trained checkpoints are tied to. With no artifact the value is null, not {}: 'nothing here knows' is not 'nothing saturated'. Also fixed exp(excess/T) raising OverflowError on the diverged-specialist input the cap exists to absorb.",

  "step_9_is_now_possible_and_partly_measured": "dataprep could only pack under SmolLM2, so 'shards under the selected tokenizer' had no way to run, and its manifests recorded nothing about the vocabulary that produced their ids. --tokenizer picks it and travels to the workers through a module global inherited across the fork, like _ACTIVE_MIXTURE_BY_KEY, rather than an argument threaded through the pool queue, the respawn path and the crash-recovery path -- a rebuild that silently fell back would look exactly like one that worked. The fingerprint lands in source_provenance beside the filters and the served commit. Default is None all the way into the worker, so every existing tree, including the 49,152 one phase 8 continues from, is byte-identical.",

  "the_guards_that_read_it": "assert_manifest_tokenizer had NO production caller before this turn. Now: train.py checks the row count (it has no tokenizer -- training reads ids and an embedding table) per source, in Trainer, before the sampler exists; scripts/bpb_eval.py checks the full fingerprint, because there the tokenizer decodes the bytes BPB is per and is therefore part of the measurement. Live check: bpb_eval pointed at the rebuilt 32,768 tree with the default tokenizer now refuses before the model is built, naming the directory and both vocabularies, where it would have returned a finite, plausible, wrong BPB. Both are inert for a manifest with no fingerprint, which is every tree on this box.",

  "finding_the_criterion_that_could_not_pass": "manifest_provenance_verdict read source_release.resolved_commit. resolve_source_release writes the served commit as sha. No writer produces that key, so the criterion was UNPASSABLE BY CONSTRUCTION -- invisible because the as-built manifests carry no source_release at all, so it failed for the reason it was designed to and this sat underneath. The test fixture had been written to the criterion's expectation rather than to the writer's output, so the test agreed with the code and both disagreed with reality. tests/test_corpus_gate.py now builds the manifest through source_provenance and reads it with the criterion. Found by a 200k-token rebuild, not by reading. ANYTHING ELSE HERE THAT READS A MANIFEST FIELD BY A NAME NOBODY WRITES HAS THE SAME SHAPE OF BUG; the fixture-agrees-with-the-code pattern is what to look for.",

  "what_the_rebuild_measured": "runs/corpus/phase7-gate-rebuild-smoke.json: one source (everyday-conversations, 69,438 tokens from 400 docs) rebuilt under data/tokenizer-lab/tokenizers/v32768 against the frozen 1,371,773-n-gram index, re-scanned with its own contam artifact, then gated. corpus-contamination PASS (zero hits over 93.34% of the tree by tokens; the criterion reports a 1.01e-2 upper bound on the document rate rather than proof of zero) and manifest-provenance PASS. THE OTHER THREE PASS TRIVIALLY AND ARE NOT EVIDENCE ABOUT A FULL REBUILD: one source at a budget it can fund cannot skew from its own target or exceed an epoch cap. The as-built verdicts were re-run against the fixed criterion and are unchanged in every number: 11.4593 pts skew, 157,921,561 unscanned fineweb-edu tokens, 10 of 10 manifests pinning nothing.",

  "next_1": "PHASE 8 IS THE REMAINING DELIVERABLE AND THE LONGEST. At T+51h with ~85h to finalization, the plan's own schedule (104-136h for Daedalus-Code) is now the late bound rather than the plan. Step 1 is the branch boundary: create vast/daedalus-code-20260824 from the current tested SHA, record that parent SHA in a code run manifest, and open the stacked draft PR to vast/daedalus-improvements-20260824 BEFORE any code-specific implementation. THE WRAPPER CAN NOW DO THIS -- `daedalus-approved branch vast/daedalus-code-20260824` prints the parent SHA -- BUT ONLY AFTER THE OPERATOR INSTALL BELOW; the installed copy is stale, so check with `daedalus-approved branch` first and expect 'unapproved command' until then. DO NOT switch the working tree while a phase is running: the detached controller reads scripts from it. Nothing is running now.",

  "next_2": "Then daedalus/codeprep.py and the code corpus: 65% permissively licensed code (Python 55%, JS/TS 12%, C/C++ 10%, Rust 8%, Go 6%, Java 5%, shell/SQL/other 4%), 15% technical/math prose, 20% general replay, split train/holdout BY REPOSITORY, decontaminated against HumanEval+/MBPP+ prompts, solutions, tests and repo metadata. It MUST pack under the 49,152 vocabulary -- phase 8 continues from released weights -- and the new trainer guard will now refuse a mix-up rather than train through it. The fetch is the long pole and is CPU/network, so it can run on the inspect lane beside a GPU phase.",

  "next_3": "Step 9 at full scale is still open and is a judgement call, not a gate. What ran is a 200k-token demonstration. A real rebuild re-streams from the Hub and re-tokenizes; the trees under data/shards are FETCHES of the released build (subset_of in every manifest), so nothing here can be re-manifested in place. A 32,768 rebuild is V2-only and trains nothing in this program. Decide it on measured throughput against what phase 8 needs, and prefer phase 8 if they contend.",

  "traps_still_current": "THE APPROVED WRAPPER MUST BE CALLED BY ABSOLUTE PATH: /usr/local/bin/daedalus-approved. Denied to this session's Bash: rm, cat, ls outside the repo, sleep/polling loops, the Monitor tool, `bash ops/vast/run-approved`, `python -c`, nvidia-smi, and reading /workspace/daedalus-progress. Allowed: ls/head/tail/grep/sed/wc/date inside the repo, git log/status/show/diff, and the wrapper. To WAIT without sleep: `tail -n 0 -f <log> | head -n 4` blocks until four more lines and exits. scripts/mixture_opt.py's --tag/--shape/--run-root/--report-root are TOP-LEVEL and must precede the subcommand; corpus_gate.py has no such split; `phase --lane inspect run-phase ...` puts --lane BEFORE the subcommand. pytest here has no --timeout plugin. A generated .md ending in a blank line cannot be committed (git diff --check); contam_scan.py was fixed, other report writers may not be.",

  "trap_the_operator_step_now_blocks_three_things": "STILL OUTSTANDING, one operator step: `bash ops/vast/install_supervisor.sh`. The installed /usr/local/bin/daedalus-approved still answers 'unapproved command pr-find', so no session can learn the PR number to apply runs/vast-program/pr-body.md; the running progress publisher still imports the pre-lanes github_progress.py, which is why STATUS.md keeps omitting the lanes field; and as of a9983ad the wrapper's new `branch` command -- the only way a session can reach vast/daedalus-code-20260824, since it cannot run git checkout itself -- is also waiting on it. PHASE 8 STEP 1 IS BLOCKED ON THAT ONE COMMAND. It is an operator step by design: an approved wrapper that changed when a branch edited a file would let a session widen its own permissions. pr-body.md is current through this turn.",

  "deadline": "Program started 2026-08-24T10:44:35Z. At this note ~T+51.0h of 144h; finalization opens at T+136h = 2026-08-30T02:44Z, leaving ~85h. Phases 0-6 are complete, phase 7 is complete except step 9 at scale, phase 8 has not started. The schedule's 104-136h window for Daedalus-Code is now a late bound, not a plan."
}
