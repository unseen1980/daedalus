{
  "state_at_note": "NO LANE RUNNING. Every lease is free, the GPU is idle and nothing is training. Main lane: phase6-stagec-go-no-go, passed 2026-08-26T06:44:05Z. Phase 6 is finished as an experiment -- stage A screened, stage B trained and gated on all five columns, and the stage-C rule answered no-go. What remains of it is the write-up, which belongs to phase 9. The next phase is 7.",

  "shipped_this_turn": "b9e7906 -- one decode report per stage, in the launcher rather than in each caller. 1ffefd3 -- stage B's recommendation, the stage-C decision, and the decode and retrieval evidence behind them. 153 focused tests (architecture_report + architecture_evidence) and 141 neighbouring (sweep, arch_space, vast_program, program_state, vast_ops) pass.",

  "the_defect": "`run_decode` refuses to overwrite a report that measures models the invocation does not -- the guard that stops a two-finalist rerun deleting a full sweep's numbers. The report name was shared by every stage while export and retrieval both write per-tag manifests, so the guard was CERTAIN to fire on the second stage to reach decode rather than protecting anything. Stage B hit it at 06:34:32Z: export and retrieval passed, decode was refused, the chain exited 1, the controller marked the phase failed, and the gate's fifth column stayed unmeasured on a stage whose eight GPU hours were already spent. The refusal message offers `--out`, and taking that would have unblocked this stage and left the next one to rediscover it; the collision is a property of the launcher, so the fix is there. Readers fall back to the legacy shared decode.json because stage A measured into that name, and the recommendation now records which file it read.",

  "what_stage_b_found": "The gate recommends NOTHING, and not on decode -- decode passes for all four arms (241-250 tok/s at depth 0, 208-226 at 2048, artifacts 101-103MB, measured on an idle box in one alternating invocation). a8-kv4, the shipped control, fails KV at 8,192 B/tok against the 6,144 ceiling. a6-kv4, a4-kv4 and a3-kv4 each fail retrieval on PASSKEY: 20.0 points under the control at depth 1024, 42.0 at 256, 38.0 at 256, against a 2-point gate. All three pass BPB, KV, export and decode. So the finding at this scale is that cutting attention layers buys 25-62% of the KV cache and costs passkey retrieval by margins nothing in the gate can absorb.",

  "the_mqar_wall_resolved": "The earlier note predicted mqar would stay at the floor at stage B and asked which honest option to take. It did: 2 of 8 control task/depth cells are still powerless, which is why the CONTROL's retrieval reads no-power. But the three candidate arms did not need mqar -- they failed on passkey, where the instrument had power. So the answer is option (a) plus (b): stage B's retrieval column rests on passkey alone, and mqar-at-proxy-scale is a named gap for the V2 recommendation. RETRIEVAL_MAX_DROP_POINTS, RETRIEVAL_MIN_ITEMS_PER_DEPTH, RETRIEVAL_POWER_FLOOR and RETRIEVAL_GATE_TASKS were not touched, before or after the numbers.",

  "stage_c": "no-go, on two of three conditions. discrimination FAIL -- 0.26 BPB points across the four arms, inside the 0.84 points their 2.48% parameter spread alone could explain at the 0.34 exponent. finalists FAIL -- every non-control arm blocked, and a measured failure does not reopen at four times the budget. deadline PASS -- 7.8 hours needed against 92.0. `--extra-hours 1.0` was measured off this stage's own timeline (scoring 05:30:25-06:08:48 including the failed first attempt, evidence chain 06:16:58-06:34:32, decode ~40s), not invented. Written up as a negative result, per the rule committed in 54900d0 before this table existed.",

  "next_1": "Phase 7, the improved general corpus and mixture. Nothing exists yet: there is no daedalus/mixture_opt.py and no scripts/mixture_opt.py. scripts/contam_scan.py and scripts/source_headroom.py do exist and the plan extends rather than replaces them. The honest first slice is to MEASURE the corpus that exists before rebuilding it -- contamination coverage, split integrity, source headroom, epoch-cap -- because every phase-7 acceptance number is stated as a delta against it.",

  "next_2": "Phase 7 has no launcher, and neither will phase 8. Whatever is written for them must go through `daedalus.supervise.run_with_resume` rather than subprocess.run, and must hand it the checkpoint path train.py actually writes -- `architecture_sweep.arm_checkpoint_path` asks train.py rather than composing it, for the reason recorded there. The mixture proxies are GPU training runs and fall under this; the corpus rebuild is CPU and disk and still wants the in-flight marker so a killed session does not restart a half-built shard set.",

  "disk_is_a_real_constraint_for_phase_7": "123G free of 915G, 86% used, before any rebuild. The plan's own answer is incremental private upload of completed source shards followed by deletion of verified-safe intermediates; that ordering is not optional here, and a rebuild that writes every source before uploading any will not fit.",

  "long_runs_outlive_the_turn": "STILL CONFIRMED WIRED, and exercised again this turn: phase6-stageb-decode was launched with --detach and ran in its own session. `scripts/vast_program.py detach_phase` spawns the phase controller with start_new_session=True; `run_with_resume` defaults resume_interrupted=True and decides from `interrupted_marker`, so a relaunch beside an open marker and an existing checkpoint carries --resume on attempt 1. qat_recovery.py:295, conv_health.py:329 and architecture_sweep.py:538 all go through it and none passes resume_interrupted=False.",

  "trap_1": "THE APPROVED WRAPPER MUST BE CALLED BY ABSOLUTE PATH: `/usr/local/bin/daedalus-approved ...`. A bare `daedalus-approved` is denied with a message that reads like the command is forbidden rather than the path.",

  "trap_2": "This session's Bash allowlist is narrow. Denied this turn: `python -c`, `python3 -c`, `tail`, `date`, heredocs, and `until ... sleep` wait loops even with run_in_background. Allowed: `ls`, `head`, `wc`, `grep` (including pipes into grep), `git log`, `git ls-files`, `df`, and the approved wrapper. Read files with the Read tool and logs with `daedalus-approved safe-log`. There is no way to sleep-poll from a session, so a detached phase is checked by re-running `ls -l runs/vast-program/*.lock` between other work.",

  "trap_3": "The evidence subcommand is `all`, NOT `chain`, despite the parser variable and help text calling it chain.",

  "trap_4": "runs/vast-program/state.json is written with sorted keys, so `lanes` sorts before `status`: grepping for status and taking the first hit answers with a lane's status, not the program's. Parse it as JSON, or grep and take the LAST `updated_at`.",

  "trap_5": "STILL OUTSTANDING. `reload-service` is in ops/vast/run-approved but the INSTALLED wrapper's invocation is denied outright, so the progress publisher cannot be reloaded from a session and STATUS.md keeps omitting the lanes field. Operator step: `bash ops/vast/install_supervisor.sh` then a reload.",

  "deadline": "Program started 2026-08-24T10:44:35Z. At this note T+44.0h of 144h; finalization opens at T+136h = 2026-08-30T02:44Z, leaving ~92h. The plan's schedule puts hours 72-104 on architecture and mixture proxies and phase 6 is finished at T+44, so phase 7 starts with slack rather than against the clock. Spend it on the corpus measurement rather than on widening a phase that has already answered."
}
