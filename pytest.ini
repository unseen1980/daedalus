[pytest]
# `slow` marks tests that spawn real subprocesses or otherwise take seconds
# rather than milliseconds. Registered so they can be skipped while a
# RAM/CPU-sensitive job (dataprep, training) has the box:
#     pytest -m "not slow"
markers =
    slow: spawns subprocesses or takes seconds; skip while a long job is live
